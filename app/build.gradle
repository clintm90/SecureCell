apply plugin: 'com.android.application'

android {
    signingConfigs {
        config {
            keyAlias 'securecell'
            keyPassword KEY_PASSWORD
            storeFile file('C:/Users/clint/clintmourlevat.jks')
            storePassword KEYSTORE_PASSWORD
        }
    }
    compileSdkVersion 21
    buildToolsVersion "21.1.2"
    def versionPropsFile = file('version.properties')
    def Properties versionProps = new Properties()
    versionProps.load(new FileInputStream(versionPropsFile))
    def runTasks = gradle.startParameter.taskNames
    def versionBuild = versionProps['VERSION_BUILD'].toInteger() + 1
    versionProps['VERSION_BUILD'] = versionBuild.toString()
    versionProps.store(versionPropsFile.newWriter(), null)
    packagingOptions {
        exclude 'META-INF/LICENSE.txt'
        exclude 'META-INF/NOTICE.txt'
    }
    defaultConfig {
        applicationId "com.securecell.core"
        minSdkVersion 15
        targetSdkVersion 21
        versionName "1.1.${versionBuild}"
        signingConfig signingConfigs.config
    }
    buildTypes {
        release {
            minifyEnabled true
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
    productFlavors {
    }

    repositories {
        maven {
            url "https://jitpack.io"
        }
    }
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])

    //google
    compile 'com.android.support:appcompat-v7:21.0.3'
    compile 'com.android.support:support-v4:21.0.'
    compile 'com.android.support:cardview-v7:21.0.3'
    compile 'com.android.support:recyclerview-v7:21.0.3'
    compile 'com.google.android.gms:play-services:6.5.87'

    //proxy
    compile 'be.shouldit:android-proxy-library:3.0.0'

    //apache commons
    compile 'commons-codec:commons-codec:1.10'
    compile 'commons-io:commons-io:2.4'
    //compile 'commons-net:commons-net:3.3'
    compile 'org.apache.commons:commons-collections4:4.0'
    compile 'org.apache.commons:commons-proxy:1.0'

    //others
    compile 'com.googlecode.libphonenumber:libphonenumber:6.3'
    compile 'org.sufficientlysecure:openpgp-api:7.0'
    compile 'org.sufficientlysecure:donations:2.3'
    compile 'com.nononsenseapps:filepicker:2.0.5'
    compile 'com.github.rey5137:material:1.1.0'
    compile 'com.github.sromku:android-simple-storage:db8694ff7a'
    //compile 'com.github.sogyf:goja-qrcode:0.1.2'
    //compile 'de.bripkens:gravatar4java:1.2'
    compile 'de.greenrobot:eventbus:2.4.0'
    compile 'ch.acra:acra:4.6.1'
    compile 'me.tatarka.support:jobscheduler:0.1.1'
    compile 'com.shamanland:fab:0.0.8'
    compile 'com.github.jjobes:SlideDateTimePicker:v1.0.2'
    compile 'net.rdrei.android.dirchooser:library:2.1@aar'

    //local
    compile files('libs/android proxy library.jar')
    compile files('libs/file_chooser.jar')
}